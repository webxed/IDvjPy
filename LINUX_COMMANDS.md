# Справочник основных консольных команд Linux

Список команд сгруппирован по тегам с **фиксированным порядком tid**. В приложении IDvjPy_term после заполнения БД ссылки вида `!file[1]`, `!net[2]` будут всегда соответствовать одной и той же команде.

**Как использовать в приложении:**
- `?` — список всех тегов
- `?file` — команды тега file с tid
- `!file[1]` — подставить команду file с tid 1 в строку ввода
- `!! file[1]; net[2]` — собрать и выполнить цепочку команд

**Заполнить БД этими командами:**  
Выполните в корне проекта: `python seed_linux_commands.py --seed`

Соответствие tid и команд в скрипте совпадает с таблицами ниже.

---

## file — файлы и каталоги

| tid | Команда | Назначение |
|-----|---------|------------|
| 1 | `ls -la` | Список файлов с деталями |
| 2 | `cp -r src dest` | Копирование рекурсивно |
| 3 | `mv src dest` | Перемещение или переименование |
| 4 | `rm -i` | Удаление с подтверждением |
| 5 | `mkdir -p` | Создать каталог и родителей |
| 6 | `rmdir` | Удалить пустой каталог |
| 7 | `touch` | Создать пустой файл / обновить время |
| 8 | `cat` | Вывести содержимое файла |
| 9 | `less` | Постраничный просмотр |
| 10 | `head` | Первые строки вывода |
| 11 | `tail -f` | Последние строки, следить за файлом |

В приложении: `!file[1]` … `!file[11]`.

---

## net — сеть

| tid | Команда | Назначение |
|-----|---------|------------|
| 1 | `ss -tulnp` | Сокеты (порты) TCP/UDP |
| 2 | `ping -c 3` | Проверка доступности хоста |
| 3 | `curl -sI` | HTTP‑запрос, только заголовки |
| 4 | `wget -qO-` | Скачать в stdout |
| 5 | `ssh` | Подключение по SSH |
| 6 | `rsync -avz` | Синхронизация по сети |
| 7 | `scp` | Копирование через SSH |
| 8 | `ip addr` | Адреса интерфейсов |
| 9 | `ip route` | Таблица маршрутизации |

В приложении: `!net[1]` … `!net[9]`.

---

## proc — процессы

| tid | Команда | Назначение |
|-----|---------|------------|
| 1 | `ps aux` | Список процессов |
| 2 | `top` | Интерактивный монитор процессов |
| 3 | `htop` | Удобный монитор (если установлен) |
| 4 | `kill` | Завершить процесс по PID |
| 5 | `killall` | Завершить по имени |
| 6 | `pkill` | Завершить по шаблону имени |
| 7 | `nohup` | Запуск, устойчивый к разрыву сессии |
| 8 | `jobs` | Список фоновых заданий оболочки |
| 9 | `fg` | Вернуть задание на передний план |
| 10 | `bg` | Продолжить в фоне |

В приложении: `!proc[1]` … `!proc[10]`.

---

## sys — система

| tid | Команда | Назначение |
|-----|---------|------------|
| 1 | `uname -a` | Информация о ядре и системе |
| 2 | `uptime` | Время работы и нагрузка |
| 3 | `free -h` | Память (ОЗУ/своп) |
| 4 | `df -h` | Место на дисках |
| 5 | `date` | Текущие дата и время |
| 6 | `whoami` | Текущий пользователь |
| 7 | `id` | UID, GID и группы |
| 8 | `env` | Переменные окружения |
| 9 | `systemctl status` | Статус служб systemd |
| 10 | `journalctl -xe` | Журнал systemd, последние записи |

В приложении: `!sys[1]` … `!sys[10]`.

---

## find — поиск

| tid | Команда | Назначение |
|-----|---------|------------|
| 1 | `find . -name "*.py"` | Поиск по имени |
| 2 | `find . -type f -mtime -7` | Файлы, изменённые за 7 дней |
| 3 | `find . -type d` | Только каталоги |
| 4 | `locate` | Быстрый поиск по имени (обновить: updatedb) |
| 5 | `which` | Путь к исполняемому файлу команды |
| 6 | `whereis` | Бинарник, исходники, man |

В приложении: `!find[1]` … `!find[6]`.

---

## archive — архивы

| tid | Команда | Назначение |
|-----|---------|------------|
| 1 | `tar -xvf` | Распаковать tar |
| 2 | `tar -cvf` | Создать tar‑архив |
| 3 | `tar -xzvf` | Распаковать tar.gz |
| 4 | `tar -czvf` | Создать tar.gz |
| 5 | `gzip` | Сжать файл |
| 6 | `gunzip` | Разжать .gz |
| 7 | `zip -r` | Создать zip рекурсивно |
| 8 | `unzip` | Распаковать zip |

В приложении: `!archive[1]` … `!archive[8]`.

---

## text — текст и потоки

| tid | Команда | Назначение |
|-----|---------|------------|
| 1 | `grep -r` | Рекурсивный поиск по тексту |
| 2 | `grep -E` | Поиск по регулярному выражению |
| 3 | `sed -i` | Редактирование файла на месте |
| 4 | `awk` | Обработка полей и строк |
| 5 | `sort` | Сортировка строк |
| 6 | `uniq` | Уникальные подряд идущие строки |
| 7 | `wc -l` | Подсчёт строк |
| 8 | `cut` | Вырезать поля |
| 9 | `tr` | Замена/удаление символов |
| 10 | `xargs` | Подставить stdin в аргументы команды |

В приложении: `!text[1]` … `!text[10]`.
