[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = src
filterwarnings =
    ignore::DeprecationWarning
markers =
    slow: long-running Pilot suites; run the quick set with -m "not slow"
    release: release-metadata consistency (docs must name CommandRunner.VERSION)
