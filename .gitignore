# PyInstaller
dist/
build/
*.spec

# Virtual environment
.venv/
venv/
pyvenv.cfg
lib/
lib64
bin/
include/

# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Local runtime (command DB, env, session logs)
*.db
.bashrc_term
.bashrc_term_*
!.bashrc_term.example
!src/.bashrc_term.example
*.txt
# Корневые скретч-файлы (test_ses.txt и т.п.); tests/ не трогаем
/test*.*
llm_providers.yml
!src/llm_providers.example.yml
# Личные настройки: копия src/settings.example.yml, создаётся при первом запуске.
# В репозиторий не попадают (settings.example.yml остаётся).
/settings.yml

# pip packaging: generated by packaging/build_wheel.sh
packaging/idvjpy_boot/src/
packaging/dist/
*.egg-info/

# Exports / backups produced by the app
*.json
!pyrightconfig.json
*.csv
backups/
.
# OS / editor
.DS_Store
*.swp
.codegraph
.ruff_cache/
