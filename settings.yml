# Maximum number of lines to keep in the output buffer.
max_lines: 100000
history_lines: 20
database_tags_file: history_v2.db
backup_dir: backups

# Таймаут выполнения команды (секунды). 0 = без таймаута (tsh login, долгие команды).
command_timeout: 10
